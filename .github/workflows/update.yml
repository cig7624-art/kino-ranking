name: Find Search API

on:
  workflow_dispatch:

jobs:
  find-search-api:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install packages
        run: |
          pip install playwright
          playwright install chromium

      - name: Find Search API
        run: python find_search_api.py

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: search-graphql-calls
          path: search_graphql_calls.json
